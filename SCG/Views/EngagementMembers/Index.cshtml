@model IEnumerable<SCG.Models.EngagementMember>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Leader)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Engagement.Location)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Guid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Leader)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Engagement.Location)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Guid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
